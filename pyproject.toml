[project]
name = "personal-site-template"
version = "0.1.0"
description = "A Markdown-first personal website built with Pelican."
requires-python = ">=3.12"
dependencies = [
    "pelican[markdown]>=4.12,<4.13",
]

[tool.uv]
package = false
